---
"@wardnet/ui": minor
---

SegmentedTabs: add optional per-tab `testId`, forwarded as `data-testid` on each tab button, so consumers can target a specific tab in e2e specs without depending on its label text.
